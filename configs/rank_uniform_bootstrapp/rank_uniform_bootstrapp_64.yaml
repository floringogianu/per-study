_experiment_parameters:
  experience_replay:
    __name: rank-boot
    alpha: 0.8
    batch_size: 32
    bayesian: true
    boot_no: 20
    boot_p: 0.8
    boot_vote: false
    sampling: rank
  mdp_size: 6
experience_replay:
  __name: rank-boot
  alpha: 0.8
  batch_size: 32
  bayesian: true
  boot_no: 20
  boot_p: 0.8
  boot_vote: false
  sampling: rank
mdp_size: 6
title: experience_replay=rank-boot; mdp_size=6
